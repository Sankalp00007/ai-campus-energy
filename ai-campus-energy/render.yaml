sites:
  - name: ecocampus-ai
    buildCommand: npm install && npm run build
    publishDir: dist
    envVars:
      - key: API_KEY
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html